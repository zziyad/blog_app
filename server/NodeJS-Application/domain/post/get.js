({
  method: async (ctx, name) => {
    console.log({ api });
    // const { repository } = domain.post;
    // const post = await repository.get(ctx, postId);
    // // if (!chat) throw new domain.Errors.NotFoundError('No such chat', { name });
    // console.log({ post });
    return 'POSTTT';
  },
});
